mixin displayTransform(name, options)
  pre
    code(class=attributes.pre?'lang-' + attributes.pre:'')
      block
  pre
    code(class=attributes.post?'lang-' + attributes.post:'')
      +filter(buf, name, options)
        block